export interface CitiesCards {
    id: number,
    name: string,
    region: string,
    year: number,
    population: number,
    oldnames: string,
    photos: [string, string, string, string],
    description: string,
    landmarks: [],
}
